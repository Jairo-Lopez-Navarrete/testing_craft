{% extends '_layout.twig' %}

{% block content %}
    <section class="hero">
        <div class="overlay">
            <div class="text-container">
                <h1>{{ entry.herotitle }}</h1>
                <p>{{ entry.herodescription }} </p>
                <a href="{{ siteUrl }}producten.html" class="button">{{ entry.herobutton }}</a>
            </div>
        </div>
    </section>
{% endblock %}